{
  "name": "context-optimize",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local-first context pressure reduction for OpenClaw",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0"
  }
}
